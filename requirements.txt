# Core ML Libraries (CPU-only versions)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Gradient Boosting (Essential for testing)
xgboost>=1.7.0
lightgbm>=4.0.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# Music Processing (Essential for generation)
music21>=9.1.0
mido>=1.3.0

# System Monitoring (Essential for resource tracking)
psutil>=5.9.0

# Data Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Deep Learning (CPU-only versions)
torch>=2.0.0+cpu --index-url https://download.pytorch.org/whl/cpu
torchaudio>=2.0.0+cpu --index-url https://download.pytorch.org/whl/cpu
tensorflow-cpu>=2.13.0

# Jupyter Notebooks
jupyter>=1.0.0
ipykernel>=6.25.0
notebook>=7.0.0

# Model Training & Monitoring (Optional - can skip tensorboard/wandb)
# tensorboard>=2.13.0  # Skip to avoid TensorFlow GPU dependencies
# wandb>=0.15.0        # Skip for now
tqdm>=4.65.0

# Data Handling
h5py>=3.9.0
hdf5storage>=0.1.18

# Utilities
scipy>=1.11.0
joblib>=1.3.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Optional: For advanced audio analysis (Skip problematic ones)
# madmom>=0.16.1      # May have compilation issues
# essentia>=2.1b6.dev858  # May have compilation issues

# Development Tools
pytest>=7.4.0
black>=23.7.0
flake8>=6.0.0

# Presentation Generation
python-pptx>=0.6.21
mistune>=2.0.0
